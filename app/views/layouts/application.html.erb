<!DOCTYPE html>
<html>
<head>
  <title>Bip</title>
  <link rel="stylesheet" href="http://twitter.github.com/bootstrap/1.4.0/bootstrap.min.css">
  <%= stylesheet_link_tag "bip.css", "tipsy.css" %>
  <link href='http://fonts.googleapis.com/css?family=PT+Sans&amp;subset=latin' rel='stylesheet' type='text/css'>
  <%= javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jquery/1.4.2/jquery.min.js", "http://ajax.googleapis.com/ajax/libs/jqueryui/1.8.1/jquery-ui.min.js", "rails.js", "http://twitter.github.com/bootstrap/1.4.0/bootstrap-alerts.js", "jquery.dataTables.min.js", "http://twitter.github.com/bootstrap/1.4.0/bootstrap-twipsy.js", "jquery.timeago.js", "application.js" %>
  <%= csrf_meta_tag %>
</head>
<body>
	<!--
	<header>
		<h1>
			<a href="<%= root_path %>">
				<span class="name">♪ BIP</span>
				<span class="explanation">Bignetwork Internet Player</span>
			</a>
		</h1>
		<%# ♫  ♪ %>
	</header>
	-->

	<!--
	<nav>
		<ul>
			<li><%= link_to 'Alla låtar', tracks_path, { :class => ("current" if current_page?(tracks_path)) } %></li>
			<li><%= link_to 'Spellistor', playlists_path, { :class => ("current" if controller.controller_name == 'playlists') || !params[:playlist_id].blank? } %></li>
			<li><%= link_to 'Sök', '/search', { :class => ("current" if controller.controller_name == 'search') } %></li>
		</ul>
	</nav>
	-->

	<div class="topbar">
      <div class="fill">
        <div class="container">
          <a class="brand" href="<%= root_path %>">
          	♪
          	Bignetwork Internet Player
          </a>
          <ul class="nav">
            <li class="<%= "active" if controller.controller_name == 'playlists' %>">
            	<a href="<%= playlists_path %>">Spellistor</a>
            </li>
            <li class="<%= "active" if current_page?(tracks_path) %>">
            	<a href="<%= tracks_path %>">Alla låtar</a>
            </li>
          </ul>
          <form method="get" action="/search" class="pull-right">
          	<% if @playlist %>
          	<input type="hidden" name="playlist_id" value="<%= @playlist.id %>" />
          	<% end %>
            <!--<label for="add_playlist_search_q">Lägg till låt:</label>-->
            <input name="q" id="add_playlist_search_q" type="search" placeholder="Sök efter artist/album/låt" value="<%= h params[:q] %>" />
            <!--<button class="btn" type="submit">Sök</button>-->
          </form>
        </div>
      </div>
    </div>

	<div class="container">

	<%= render_crumbs :format => :html_list, :ul_class => 'breadcrumb', :active_li_class => 'active', :separator => '<span class="divider">/</span>'  %>

	<% flash.each do |key, msg| %>
		<div class="alert-message <%= key %>" data-alert="alert">
  			<a class="close" href="#">×</a>
  			<p>
  				<strong><%= key.capitalize %></strong><br/>
  				<%= msg %>
  			</p>
		</div>
	<% end %>

	<section id="page" class="<%= controller.controller_name %> <%= controller.action_name %>">

		<%= yield %>
		
	</section>
	
	<aside>
		<div class="row">
		<div id="updates" class="span7"> 
			<h3>Aktivitet:</h3> 
			<ul id="activities"> 
				<% if @latest_votes.blank? %>
					Ingen har röstat på någon låt ännu.
				<% else %>
					<% @latest_votes.each do |vote| %>
					<li class="activity"> 
						<span class="icon vote">❤</span> 
						<%= link_to 'Någon', '#' %>
						röstade på låten
						<% if vote.playlist_item && vote.playlist_item.track %>
							<%= link_to "#{vote.playlist_item.track.artist} - #{vote.playlist_item.track.name}", vote.playlist_item %>
						<% else %>
							?
						<% end %>
						<%= timeago vote.created_at, :class => 'timestamp timeago', :inner_html => "för #{time_ago_in_words vote.created_at, true} sedan" %>
					</li> 
					<% end %>
				<% end %>
			</ul> 
		</div> 
		<div id="about" class="span7"> 
			<h3>Vad är BIP?</h3> 
			<p>I BIP kan man lägga upp och rösta på låtar man gillar. Låtarna med flest röster hamnar överst och spelas först.</p> 
			<p>Musiken kommer från <a href="http://www.spotify.com/">Spotify</a> och på sikt hoppas vi kunna göra samma sak med YouTube-videor.</p> 
		</div>
		</div>
	</aside> 

	<footer> 
		<p>BIP, Bignetwork Internet Player – en produktion av <a href="http://www.bignetwork.se/bst">Bignetwork Software Team</a></p>
	</footer>

	</div> <!-- /container -->
	
	<%= yield :javascript %>
        <script type="text/javascript">
var disqus_shortname = 'bignetworkinternetplayer';
(function () {
  var s = document.createElement('script'); s.async = true;
  s.src = 'http://disqus.com/forums/bignetworkinternetplayer/count.js';
  (document.getElementsByTagName('HEAD')[0] || document.getElementsByTagName('BODY')[0]).appendChild(s);
}());
</script>

</body>
</html>
